openai>=1.40.0
pydantic>=2.7.0
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pytest>=8.0.0
pytest-mock>=3.14.0

# Used only by scripts/estimate_page_cost.py and one-off report verification.
# PDF rendering itself uses Chromium (via Playwright), not a Python dependency.
pypdf>=4.0.0
